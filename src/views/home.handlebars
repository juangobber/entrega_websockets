<div>
    <h1>Estos son los productos</h1>
    <div>
        <ul>
        {{#each data}}
        <li>{{this.title}} - {{this.description}} - ${{this.price}}</li>
        {{/each}}
        </ul>
    </div>
</div>
